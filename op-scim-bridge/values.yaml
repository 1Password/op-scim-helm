scim:
  name: "{{ .Chart.Name }}-bridge"
  version: "{{ .Chart.AppVersion }}"
  credentialsVolume:
    name: "{{ .Chart.Name }}-bridge-credentials"
    file: scimsession
    type: emptyDir
    values: {}
    # storageClass: do-block-storage
  # credentialsSecret:
  #   name: "{{ .Chart.Name }}-bridge-credentials"
  #   key: scimsession
  #   value: "base64 encoded scimsession file"
  # TODO: nest all image* keys
  imageRepository: 1password/scim
  imagePullPolicy: Always
  imagePullSecrets: []
  httpPort: 8080
  httpsPort: 8443
  serviceType: LoadBalancer
  probes:
    liveness:
      enabled: true
      path: "/ping"
  config:
    redisURL: "redis://{{ .Chart.Name }}-bridge-redis-master:6379"
    # domain: example.1password.com
    # letsEncryptDomain: scim.example.com
    # debug: false
    # jsonLogs: false
    # prettyLogs: false
    # oneClick: false # TODO: confirm if this is even a thing
  resources: {}
  annotations: {}
  labels: {}
  podAnnotations: {}
  podLabels: {}
  nodeSelector: {}
  affinity: {}
  tolerations: []

redis:
  enabled: true
  image:
    registry: docker.io
    repository: bitnami/redis
    tag: latest
    pullPolicy: IfNotPresent
  cluster:
    enabled: false
  usePassword: false
